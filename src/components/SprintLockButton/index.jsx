import LockIcon from "@mui/icons-material/Lock"
import { IconButton } from "@mui/material"

const SprintLockButton = () => {
    return (
      <IconButton color="error">
        <LockIcon />
      </IconButton>
    )
}

export default SprintLockButton
